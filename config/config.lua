local Config = {}

-- Save system
Config.Save = {
    File = "LuiSave.json",
    AutoPlayerInfo = true
}

-- Keybinds (UI toggle)
Config.Keybind = {
    ToggleUI = "RightShift" -- change as needed
}

-- Key system (for cheats / external)
Config.Keysystem = {
    Enabled = true,
    Key = "prolokmn",
    Multiplier = 7
}

return Config

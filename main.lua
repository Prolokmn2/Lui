local Config = require(script.config.config)
local Init = require(script.ui.init)
local Debug = require(script.ui.debug)

Debug:Log("Starting Lui")

local window = Init:Start()

Debug:Log("Loaded UI")
